# Project Overview

Legacy compatibility note: the canonical project brief now lives in `docs/context/PROJECT_BRIEF.md`.

Keep this file as an alias because earlier repository instructions referenced `project-overview.md`.
