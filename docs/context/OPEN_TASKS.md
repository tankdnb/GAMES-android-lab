# Open Tasks

## Active

- Choose the root repository license before the first public GitHub push.
- Create the public GitHub repository and add `origin`.
- Push local branch `main` as the default public branch.

## Next Recommended

- Start the second research batch with up to 4 repositories not already listed in `research/registry/RESEARCHED_REPOS.md`.
- If the next batch stays lightweight, include one Android-native game, one engine/framework candidate such as `littlektframework/littlekt`, one rendering/tooling-oriented repository, and one gameplay-heavy wildcard.
- If `yairm210/Unciv` is selected next, isolate it as a heavy-repo batch with strict scope and no extra large clones in the same pass.

## Deferred

- Deeper subsystem follow-up inside already accepted repositories unless future product work needs it.
- More aggressive third-party build execution until the lightweight static-first workflow proves worth extending.
